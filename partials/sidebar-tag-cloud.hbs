{{!-- Tag Cloud --}}
{{#get "tags" limit="10" include="count.posts" order="count.posts desc"}}
    <div class="sidebar-items u-card-shadow">
        <h3 class="sidebar-title">{{t "Tags"}}</h3>
        {{#foreach tags}}
            <a href="{{url absolute="true"}}" class="btn u-tags i-tag">{{name}}</a>
        {{/foreach}}
    </div>
{{/get}}
