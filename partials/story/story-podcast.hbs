<article class="col s12 m6 l12 u-flex">
    <div class="spc-c u-relative u-marginBottom30">

        {{#if feature_image}}
        <figure class="spc-c-img u-bgCover u-relative">
            <img class="u-absolute u-image u-block lazyload"
                data-srcset="{{img_url feature_image size="xs"}} 100w,{{img_url feature_image size="s"}} 300w"
                data-src="{{img_url feature_image size="s"}}"
                alt="{{title}}"
            />
        </figure>
        {{/if}}

        <div class="spc-c-body">
            {{> "helper/helper-story-meta"}}
            <h2 class="spc-c-title u-marginBottom20 u-fontWeightMedium u-fontSize26 u-lineHeightTight"><a href="{{url}}" class="u-relative zindex3">{{title}}</a></h2>
            <p class="spc-c-excerpt u-marginBottom20 card-excerpt">{{excerpt}}</p>
            <a href="{{url}}" class="listen-btn button u-textUppercase u-fontSizeSmall u-relative zindex3">
                <span class="listen-icon svg-icon">{{> "icons/icon-listen"}}</span>
                {{t "Listen"}}
            </a>
        </div>

        <a href="{{url}}" class="u-absolute0 zindex2"></a>
    </div>
</article>
